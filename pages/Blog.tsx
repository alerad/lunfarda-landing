import * as React from "react";

interface BlogProps {}

export const Blog: React.FC<BlogProps> =
    () => (
        <React.Fragment>
            Blog off
        </React.Fragment>
    )

export default Blog